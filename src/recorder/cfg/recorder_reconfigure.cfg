from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("enable", bool_t, 1, "Enable recording", False)
gen.add("data_folder", str_t, 2, "Folder to store collected data", "~/donkaycar_data")
gen.add("downsample_rate", int_t, 4, "Downsampling rate for incoming images", 1, 0)
gen.add("record_button", int_t, 8, "Button to start/stop recording", 5, 0)

exit(gen.generate("recorder", "recorder_node", "recorder"))